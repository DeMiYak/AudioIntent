# Отчёт: извлечение выражений установления и прекращения контакта

## 1. Постановка задачи

Задача — автоматически найти в русскоязычном фильме реплики, в которых персонажи **устанавливают** или **прекращают** контакт (приветствия, прощания, знакомства, завершение телефонного разговора), и представить результат в виде таблицы `Спикер - фраза` с разбивкой на `opening` / `closing`.

Входные данные: видеофайл фильма без какой-либо разметки. Выходные: Excel-файл с колонками `opening` и `closing`, совместимый с `notebooks/evaluation.ipynb`.

---

## 2. Что было сделано

Pipeline состоит из следующих этапов:

### 2.1. Подготовка обучающих данных

Из листа **Диалоги** gold-разметки (`data_val.xlsx`) извлечены 1170 реплик из 18 фильмов с метками `contact_open` (413), `contact_close` (175), `none` (582). Они использованы как для построения rule-based лексикона, так и для обучения ML-классификатора.

### 2.2. Rule-based классификатор

Построен TF-IDF лексикон: 229 open-фраз и 119 close-фраз. Дополнен `MANUAL_PATTERNS` — регулярными выражениями для устойчивых русских конструкций (алло, до свидания, увидимся, давайте знакомиться и др.), не охваченных TF-IDF из-за редкости в обучающей выборке.

**Мотивация:** rule-based подход детерминирован, интерпретируем и устойчив к отсутствию разметки на новом фильме. Он хорошо работает на однозначных фразах («пока», «здравствуйте», «до встречи»).

### 2.3. ML-классификатор

TF-IDF (char n-gram 2–5) + LogisticRegression с `class_weight='balanced'`. Обучен на тех же 1170 репликах.

Метрики на обучающей выборке:

| Класс         | Precision | Recall | F1   |
|---------------|-----------|--------|------|
| contact_open  | 0.877     | 0.831  | 0.853 |
| contact_close | 0.784     | 0.994  | 0.877 |
| none          | 0.889     | 0.851  | 0.869 |

**Мотивация:** ML расширяет покрытие за пределы фиксированного лексикона. Он улавливает контекстуальные сигналы прощания («я поеду», «мне пора»), которые не являются устойчивыми фразами, но встречаются в обучающих данных.

### 2.4. Комбинированный режим (combined)

Rule-based и ML работают независимо на одной реплике; rule-based имеет приоритет при конфликте. Итоговое предсказание — объединение результатов обоих.

**Мотивация:** combined mode компенсирует слабости каждого подхода в отдельности. Rule-based обеспечивает точность на однозначных случаях; ML добавляет охват на пограничных. На validation-выборке combined mode дал наилучший F1.

### 2.5. ASR и diarization

- **ASR:** faster-whisper (модель `medium`), запуск на Google Colab GPU.
- **Diarization:** pyannote `speaker-diarization-community-1`, отдельный venv из-за конфликтов зависимостей.
- **Speaker identification:** Resemblyzer — cosine similarity между голосовым профилем персонажа и сегментами diarization. Порог сходства 0.48, минимальная длительность сегмента 1.5 с.

Голосовые профили 14 персонажей «Питер FM» извлечены вручную из сцен с однозначно идентифицируемой речью.

### 2.6. Тестовый фильм

Фильм разбит на 9 чанков по 10 минут. После ASR + diarization запущен пост-обработка (`src/pipeline.py --scan-windows`). Результат экспортирован в `artifacts/test_piter_fm/detailed_pairs.xlsx` (`src/export_detailed_pairs.xlsx`).

Статистика по тестовому фильму:
- 9 чанков (chunk_001–chunk_009)
- 692 реплики после сборки utterances
- 403 предсказания в итоговом файле (`artifacts/test_piter_fm/detailed_pairs.xlsx`)

---

## 3. Результаты на validation-выборке

Оценка проведена на фильме «Статус: свободен» (28 validation-окон) тремя режимами классификации при одинаковых остальных параметрах (`--similarity-threshold 0.48`, `--diarization-segment-mode regular`).

### Rule-based (лексикон TF-IDF + MANUAL_PATTERNS)

| Набор   | Gold | Pred | Jaccard | Precision | Recall | F1    | matched | exact | avg sim |
|---------|------|------|---------|-----------|--------|-------|---------|-------|---------|
| all     | 48   | 28   | 0.169   | 0.393     | 0.229  | 0.290 | 18      | 6     | 0.499   |
| opening | 40   | 22   | 0.148   | 0.364     | 0.200  | 0.258 | 14      | 4     | 0.417   |
| closing | 8    | 6    | 0.273   | 0.500     | 0.375  | 0.429 | 4       | 2     | 0.783   |

Высокая точность (особенно на closing), но низкий recall — лексикон не покрывает нестандартные формулировки.

### ML (TF-IDF char n-gram + LogisticRegression, threshold=0.35)

| Набор   | Gold | Pred | Jaccard | Precision | Recall | F1    | matched | exact | avg sim |
|---------|------|------|---------|-----------|--------|-------|---------|-------|---------|
| all     | 48   | 42   | 0.098   | 0.190     | 0.167  | 0.178 | 22      | 6     | 0.539   |
| opening | 40   | 29   | 0.095   | 0.207     | 0.150  | 0.174 | 20      | 5     | 0.534   |
| closing | 8    | 13   | 0.105   | 0.154     | 0.250  | 0.190 | 2       | 1     | 0.589   |

Больше предсказаний, чем rule-based, но ниже точность. Модель обучалась на тех же данных, из которых строился лексикон, — это ограничивает её способность обобщаться.

### Combined (rule + ML, лучшая версия v11)

| Набор   | Gold | Pred | Jaccard | Precision | Recall | F1    | matched | exact | avg sim |
|---------|------|------|---------|-----------|--------|-------|---------|-------|---------|
| all     | 48   | 52   | 0.136   | 0.231     | 0.250  | 0.240 | 34      | 8     | 0.483   |
| opening | 40   | 36   | 0.152   | 0.278     | 0.250  | 0.263 | 27      | 7     | 0.500   |
| closing | 8    | 16   | 0.091   | 0.125     | 0.250  | 0.167 | 7       | 1     | 0.417   |

Combined даёт наибольшее число matched (34 vs 22 у ML и 18 у rule-based) и лучший F1 на opening. Recall растёт за счёт суммирования покрытия обоих подходов, однако точность падает из-за накопления ложных срабатываний из обоих источников.

**Общий вывод:** основная причина невысоких метрик — качество speaker attribution. Ошибка в имени спикера приводит к несовпадению с gold даже при правильно найденной фразе. Дополнительно: rule-based мисфайрит на разговорных словах («давай», «алло» в нейтральном контексте), ML генерирует ложные срабатывания на низких уровнях уверенности.

---

## 4. Анализ ошибок на тестовом фильме

На тестовом фильме gold-разметка пока недоступна, поэтому анализ основан на визуальном просмотре `detailed_pairs.xlsx`.

### 4.1. Завышенное число событий

403 события на 90-минутный фильм (~4.4 в минуту) — вероятно, в 2–4 раза больше реального числа.

Тип
установление контакта    273
прекращение контакта     130

**Причина:** низкий порог ML-классификатора (0.35) намеренно смещён в сторону полноты охвата. Rule-based лексикон срабатывает на частые разговорные слова («давай», «ну», «алло») даже вне контекста приветствия.

### 4.2. Двойная метка opening + closing у одной реплики

Ряд реплик одновременно попал в оба типа (например, «Давай я поеду» у Пети, 00:05:11). Rule-based определил «давай» как OPEN_STRONG, ML — как closing. Оба результата объединяются в combined mode без взаимоисключения.

При конфликте между rule-based и ML следует выбирать один класс — предположительно тот, что даёт ML, как более контекстуально обоснованный.

### 4.3. Ограничения speaker identification

14 голосовых профилей — много для Resemblyzer. При 6 diarization-спикерах на чанк и пороге 0.48 часть реплик остаётся с меткой `unknown`. Некоторые персонажи с похожими голосами (Маша / Марина) могут путаться.

---

## 5. Что можно улучшить

- **Повысить порог ML** (например, до 0.45–0.50) для снижения ложных срабатываний — за счёт полноты.
- **Дедупликация dual-type строк:** при конфликте rule-based / ML оставлять одну метку.
- **Улучшить speaker ID:** более длинные эталонные сегменты, x-vector или ECAPA-TDNN вместо Resemblyzer.